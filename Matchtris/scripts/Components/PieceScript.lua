PieceScript = Object:extend()

function PieceScript:new(color)
  self.color = color
  self.multiplier = 1.0
  self.gridRow = 0
  self.gridCol = 0
end

function PieceScript:update(dt, gameobject)
  
end

